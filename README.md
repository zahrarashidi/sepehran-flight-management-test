# sepehran-flight-management-test
FastAPI application demonstrating advanced data manipulation (Pagination, Sorting, Filtering) using raw SQL against flight data, where a JSON file acts as the persistent storage layer.
